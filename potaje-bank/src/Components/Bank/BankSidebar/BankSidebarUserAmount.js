import React from 'react';

const BankSidebarUserAmount = (props) => {
    return (
      <div>
        <p>${props.amount}</p>
        <p>Amount Balance</p>
      </div>
    );
}

export default BankSidebarUserAmount;
